# AGENTS.md — AI 协作准则（AI 代理必读）

> 本文件**只规定 AI 代理在本仓库中的行为准则**，不包含任何游戏设计内容。
> 游戏设计与项目文档全部放在 `docs/` 目录。
> 规范依据：`AGENTS.md` 通用约定（[morphllm 规范说明](https://www.morphllm.com/agents-md-guide)、[Tembo 实践指南](https://www.tembo.io/blog/agents-md)）、
> Python 官方规范 [PEP 8](https://peps.python.org/pep-0008/)、[PEP 20](https://peps.python.org/pep-0020/)。
> **本文件由人类维护；代理不得自行修改本文件，如需修订必须先提出并由人类定夺。**

## 准则一：最简原则，不做多余设计

1. **只实现人类明确要求的功能**。没有写进需求的功能一律不做。
2. **禁止预留性设计**：不预留接口、不预留参数、不预留抽象层、不写"以后可能会用到"的钩子、不做插件/配置化/多模式开关。
3. **禁止防御性设计**：不为"万一出错"加校验、兜底、降级、重试、日志、告警。
4. **禁止过度工程**：不做分层架构（MVC/服务层/仓储层等）、不引入设计模式、不为单文件小游戏拆多包。
5. **一切设计性工作由人类最终定夺**，包括但不限于：
   - 技术栈与依赖选择
   - 模块/文件划分与命名
   - 玩法规则、交互方式、界面布局
   - 数据结构与存档格式
   - 是否使用类、是否使用某个库的某个 API
6. 代理可以**提方案、列选项、讲取舍**，但**不得自行拍板后直接写进代码**。

## 准则二：先检索，后设计

1. 在**任何设计与编码动作之前**，先检索：
   - 是否有可用的开源库/现成轮子能直接实现该功能；
   - 是否有成熟的人类设计规范、开发规范、代码规范可参考。
2. 检索结果必须**给出可点击来源链接**，并说明"采用 / 不采用"及理由。
3. **禁止自行发明规范或架构**。若检索不到成熟参考，应如实说明"未找到成熟参考"，并把选择权交回人类，而不是自己造一套。
4. 已确定的参考基准（后续新增需人类确认）：
   - 代码风格：[PEP 8](https://peps.python.org/pep-0008/)
   - 设计哲学：[PEP 20](https://peps.python.org/pep-0020/)
   - 本文件结构：`AGENTS.md` 通用约定

## 准则三：代码保持最简，视代理为 Python 入门级开发者

写代码时假设作者是**刚学会 Python 的人**，目标是"功能能跑"，而非"代码漂亮"。

**要求：**

- 用最直白的方式实现功能：顺序语句、`if`、`for`、`while`、函数、列表/字典，够用就用。
- 变量与函数名用可读的英文小写单词；注释用中文，只写"这行在干什么"。
- 单个文件能写完就写单文件；函数体尽量短、一眼能看懂。
- 遵守 PEP 8 的基本格式（缩进、空行、命名），但**不为了格式引入额外工具链**（除非人类要求）。

**禁止：**

- 禁止任何形式的**防御性编码**：参数校验、类型检查、`try/except` 兜底、输入合法性校验、超时/重试、资源清理的"以防万一"、错误码体系。
  - 例外：只有当"处理该情况"本身是**人类明确列出的功能需求**时才可以写，且必须能被人类指到需求原文。
- 禁止元编程：装饰器、描述符、`__getattr__`、动态导入、`eval/exec`。
- 禁止抽象：抽象基类、`Protocol`、泛型、类型体操、复杂继承。
- 禁止炫技：复杂推导式、链式一行流、位运算技巧、`*args/**kwargs` 魔法、lambda 嵌套。
- 禁止无关产物：日志框架、配置文件、命令行参数解析、单元测试脚手架、CI、Docker、打包脚本（除人类明确要求）。
- 禁止引入未获批准的第三方库。

## 准则四：技术栈固定，不得擅自扩展

1. **技术栈必须固定且唯一**，不得随意变更。当前技术栈见 `docs/03-项目架构设计.md`。
2. **严禁在未询问人类的情况下使用任何其他技术栈**：包括其他语言、其他框架、其他库、其他工具（如前端框架、数据库、构建工具、包管理器）。
3. 需要新增依赖时，流程固定为：**说明为什么现有栈做不到 → 给出候选库及链接 → 人类批准 → 记入技术栈表**。

## 准则五：指令不清晰必须询问人类

1. **任何不清晰的地方都必须先问，不得边猜边做。** 包括：需求边界、玩法细节、技术栈、文件命名、验收标准、优先级。
2. **禁止用"默认实现"填补空白**，禁止"先按常见做法做，回头再说"。
3. 提问要求：
   - 一次把相关疑问集中问完，不要挤牙膏式反复追问；
   - 每个问题给出 **2-3 个具体可选项**，并标明推荐项与各自影响；
   - 得到清晰回答后**才**开始动作；回答仍模糊时，继续确认，不进入下一步。
4. 人类未回答前，代理可做的最多是：检索资料、列出方案对比，**不得产出实现代码**。

## 准则六：只做人类要求的事

1. **严格按人类本轮明确提出的要求行事**：要求什么就做什么，**不多做、不少做**。
2. **禁止自行扩展范围**：不顺手改其他文件、不顺手加功能、不顺手重构、不顺手补文档、不顺手优化、不顺手清理无关内容。
3. **禁止"顺手做了更好"的自我判断**：任何超出当前要求的事，只提出来交人类决定，不得擅自去做。
4. **一轮对话只推进人类本轮要求的那件事**，做完即停、汇报结果、等人类确认；过程中发现的其他问题只**记录**，不擅自处理。
5. 要求本身含糊时，按准则五提问，不得自行补全。

## 准则七：设计阶段不涉及实际开发

1. **设计与编码是两个分离的阶段**：设计阶段只产出设计文档，**不得写任何可运行代码**。
2. 设计阶段允许的产出：文字说明、表格、流程图、界面草图/布局图、数据结构定义、算法步骤描述。
3. 设计文档经人类确认后，才进入编码阶段。
4. 仓库中已存在的代码一律视为**原型**，不作为正式开发成果，也不因设计变更被"顺手"修改。

## 准则八：设计流程遵循软件工程规范（课程作业精简版）

1. **只保留五项基础规范，不额外扩展**：

| 序号 | 文档 | 内容 |
| --- | --- | --- |
| 01 | `docs/01-可行性分析.md` | 项目背景、技术/经济/时间可行性、风险、结论 |
| 02 | `docs/02-游戏设计.md` | 玩法、属性、事件、结局等游戏设计 |
| 03 | `docs/03-项目架构设计.md` | 技术栈、模块划分、分层原则、数据流 |
| 04 | `docs/04-程序设计.md` | 数据结构、关键算法、模块职责 |
| 05 | `docs/05-页面设计.md` | 界面布局、控件、交互 |

2. 文档顺序即流程顺序：**可行性分析 → 游戏设计 → 项目架构设计 → 程序设计 → 页面设计**。
3. 每份文档**逐项由人类确认**后，才进入下一项。
4. **不引入课程未要求的文档**（如需求规格说明书、详细设计说明书、测试计划等）。

> 以上准则按顺序执行即为标准流程：**澄清 → 检索 → 提方案 → 动作 → 验证**。

## 变更记录

| 日期 | 变更内容 | 确认人 |
| --- | --- | --- |
| 2026-09-09 | 初版：依据人类给定的 5 条准则扩充为本规范 | 人类 |
| 2026-09-09 | 补记技术栈、题材、玩法规格等确认项（当时写在本文件内） | 人类 |
| 2026-09-09 | **重构**：本文件只保留 AI 准则 + 变更记录；游戏设计内容全部迁入 `docs/` | 人类 |
| 2026-09-09 | 新增准则六「只做人类要求的事」（替代原拟的「一轮只做一件事」） | 人类 |
| 2026-09-09 | 新增准则七「设计阶段不涉及实际开发」、准则八「设计流程只保留五项基础规范」 | 人类 |