# 02 游戏设计

> 状态：**已编写，人类已确认；按新增要求补充成就系统与回顾，待确认**（准则八）
> 本文件只写玩法规则，不含代码；实现细节（算法、字段）见 `04-程序设计.md`。

## 1. 游戏概述

| 项 | 内容 |
| --- | --- |
| 名称 | 北理工重开模拟器 |
| 原型 | 《人生重开模拟器》—— 只借鉴玩法结构，**文案与数据全部原创**，不使用其数据文件 |
| 类型 | 文字模拟 / 养成，单机 |
| 主题 | 北京理工大学本科四年 |
| 单局时长 | 约 5–10 分钟（48 回合） |
| 重开机制 | 结局页可直接「再来一局」；书院、专业、天赋、事件随机，同一套路可玩出不同结局 |
| 长期目标 | 通过成就系统与回顾功能，鼓励反复重开 |

**一句话玩法**：抽天赋 → 分配属性 → 每月触发一条事件 → 四年后按属性与经历判定结局。

## 2. 核心玩法循环

1. **首页**：开始游戏 / 成就 / 回顾 / 退出游戏
2. **开局页**：系统随机分配书院 → 从天赋池随机抽 5 个、**玩家最多选 2 个（可少选或不选）** → 玩家把 20 点分给 4 项属性（天赋与属性在同一页）
3. **每月一个回合**：按当前时段与条件筛选事件 → 随机触发 1 条 → 显示事件文案与属性变化
4. **第 13 回合（大二 9 月）**：系统在本书院可选范围内随机分配专业
5. **第 48 回合（大四 8 月）**：按硬门槛与实际经历给 11 个结局判定（详见 `docs/ending.md`）
6. **结局页**：三个按钮 —— 回顾本局 / 返回首页 / 再来一局

## 3. 属性系统

| 属性 | 分配时 | 作用 |
| --- | --- | --- |
| 智力 | 整数点，每项上限 10 | 学习、科研、竞赛类事件的判定 |
| 体质 | 整数点，每项上限 10 | 体测、军训、作息类事件的判定 |
| 颜值 | 整数点，每项上限 10 | 社交、社团类事件的判定 |
| 家境 | 整数点，每项上限 10 | 经济类事件的判定 |

- 开局 **20 点自由分配**，每项分配上限 10，**可以分不完直接开始**（人类裁定）
- **属性不封顶**：事件与天赋叠加后允许超出 0–10（人类裁定）
- **属性值允许非整数**：事件与天赋可以加减小数，**最小单位 0.1**（人类裁定）；分配时仍按整数点
- 属性变化**只由事件与天赋驱动**，没有其他来源

## 4. 书院与专业系统

- 书院共 **4 个**：睿信、求是、明德、特立
- **书院由系统随机分配**，玩家不能自选；存在可指定书院的特殊天赋
- **大一不分专业**；**第 13 回合（大二 9 月）开始时分配专业**
- **专业由系统在本书院可选范围内随机分配**，玩家不能自选
- 专业名单（人类已确认，共 20 个）：

| 书院 | 可选专业 |
| --- | --- |
| 睿信 | 计算机科学与技术、软件工程、人工智能、电子信息工程、通信工程、自动化 |
| 求是 | 数学与应用数学、应用物理学、统计学、化学、化学工程与工艺、生物技术、生物医学工程 |
| 明德 | 工商管理、经济学、国际经济与贸易、会计学、法学、英语、工业设计 |
| 特立 | 上述 20 个专业全部可选 |

- 书院与专业**参与事件判定**；专业条件只在第 13 回合之后才可能生效（大一无专业）

## 5. 天赋系统

- 开局**随机抽 5 个，玩家最多选 2 个生效**（**可少选或不选，随时可以开始**）
- 天赋分**三个等级**，每个抽取时按等级概率决定：

| 等级 | 名称 | 颜色 | 概率 | 属性加成 |
| --- | --- | --- | --- | --- |
| 蓝 | 稀有 | `#3A76D0` | 75% | +1 |
| 紫 | 史诗 | `#8052C6` | 20% | +2 |
| 金 | 传说 | `#C69418` | 5% | +4 |

- 效果分两类：
  1. **开局属性加成**（4 属性 × 3 等级 = **12 个属性天赋**，数值 +1 / +2 / +4）
  2. **提高某类事件的抽取概率**（按事件标签加权）
- 另有**指定书院**类特殊天赋
- **详细设计见 `docs/talent.md`**；当前共 **26 条**（12 属性 + 14 特殊），未达 30 条，**暂留待补**

## 6. 事件系统

- **每回合 1 条**，事件池 **≥100 条**，原创文案
- 事件分四类（人类裁定）：

| 类别 | 说明 |
| --- | --- |
| **基础事件** | 校园日常，有好有坏；含**无属性影响**的纯叙事、**保底事件**（属性变化 0.1~0.3、低权重常驻候选池）、**有前提条件**的事件 |
| **特殊事件** | 有**概率**（`chance`）与**条件**（天赋 / 属性 / 前置事件）限制，通常本局只出现一次 |
| **判定事件** | 触发后按属性掷判定，成功 / 失败得到不同文案与效果 |
| **特殊时段事件** | 军训 / 期末 / 寒假 / 暑假 / 实习 / 答辩各有**独立候选库**，与日常事件不混用 |

- **状态标志（flags）**：与结局标签分开的状态集合（如 `恋爱中`、`学生组织`）。事件用 `flags_need`（必须已具备）/ `flags_forbid`（必须不具备）作前提条件，用 `flags_set` / `flags_clear` 增删状态 —— 以此避免"未恋爱就分手""恋爱后还表白"这类矛盾
- **判定**：事件内写 `base` 与属性系数，**成功率 = base + Σ(属性值 × 系数)**，夹在 5%~95%；例如表白成功率由**颜值与家境**决定，竞赛结果由**智力**决定
- **判定的三种形式**：二选一（`success` / `fail`）、多档（`levels`，按得分取档，如竞赛奖项）、只出分（`score`，如四六级）—— 详见 `docs/event.md` §4
- 每条事件包含：文案、类型、时段、触发条件、属性变化、结局标签、状态变化
- **寒假与暑假是两个独立时段**；事件用 stage **列表**声明适用时段，命中其一即可触发 —— 因此两者**可共用同一事件池**（如 ["寒假", "暑假"]），也可各自专属；**文案必须写清属于哪个假期，不得混淆**
- 属性变化幅度：**2 以内**，大部分为**非整数小数值**（最小单位 0.1），重大事件幅度更大（人类裁定）
- 事件总数 **181 条**（基础 100 / 特殊 81；常规 96、特殊时段 85），清单见 `docs/event-list.md`
- 每个时段都有**保底事件**（`weight` 0.2），所以每月必定抽到事件；保底文案形如"这段时间过得平平淡淡，没什么可记的。"
- **详细设计见 `docs/event.md`**

## 7. 结局系统

- 结局池 **11 条**，分四类：**升学 5**（保研外校、保研本校、考研外校、考研本校、出国留学）、**就业 4**（进入大厂、创业、进入编制、援建乡村）、**学业 1**（延毕）、**兜底 1**（待业）
- 判定时机：第 48 回合
- 判定流程（**硬门槛 + 加权竞争**，人类裁定）：
  1. **延毕优先**：智力不达标直接延毕
  2. **硬门槛过滤**：不满足门槛的结局直接出局（如出国留学要求家境 > 7）
  3. **升学类内部竞争**：按竞争权重抽一个
  4. **升学胜者与就业类竞争**：再按竞争权重抽一个
  5. **待业兜底**：一条都不剩时命中待业
- **保研看平均综测排名**：保研本校要求平均排名 ≤ **40%**、保研外校 ≤ **5%**，且保外校的院校**仅限高于北理层级**的 8 所
- 竞争权重 = `weight × Σ(√因子值 × 因子权重) × 随机系数` —— **边际效用**（因子取平方根）与**随机权重**（人类裁定）；因子指某项属性值或某个标签的出现次数
- 升学院校由能力分决定：`智力 × 10 + 家境 × 2`，取不低于该分的最高档
- **详细设计见 `docs/ending.md`**，清单见 `docs/ending-list.md`

## 8. 成就系统

- 成就**跨局累计**，达成后永久保留
- 达成条件分六类（人类裁定）：
  1. **结局**：拿到某个指定结局（`value` 可为结局名或结局名列表）
  2. **事件**：经历过某条指定事件或判定结果文案
  3. **属性**：某属性达到指定数值
  4. **排名**：综测名次在指定名次以内
  5. **数值**：玩家字段达到指定值（如六级分数）
  6. **全局**：跨局累计（如走遍全国的省份数）
- 另有**开局**类成就（开始一局游戏即达成）
- 当前共 **15 条**，清单见 `docs/achievement-list.md`
- 成就页可查看全部成就与达成状态

## 9. 回顾功能

- **回顾页**：列出**最近 5 局**（每局记录书院、专业、性别、结局与所选天赋），可查看该局每月的文本与结局
- **导出**：把最近 5 局的每月文本与结局导出为 **txt 文件**
- **跨次保留**：记录与成就进度写入本地文件，关程序后再打开仍可查看（人类裁定）
- 结局页的「回顾本局」按钮可直接查看本局每月文本

## 10. 时间轴

- 第 1 回合 = 大一 9 月；第 48 回合 = 大四 8 月；每 12 回合为一个学年
- **七个时段互不重叠，没有优先级**（人类裁定）；**寒假与暑假分开**：

| 时段 | 时间 | 对应回合 |
| --- | --- | --- |
| 军训 | 大一 9 月 | 第 1 回合 |
| 期末 | 每年 1 月、6 月（**大四 6 月除外**） | 第 5、10、17、22、29、34、41 回合 |
| 寒假 | 每年 2 月 | 第 6、18、30、42 回合（共 4 个） |
| 暑假 | 每年 7 月、8 月 | 第 11–12、23–24、35–36、47–48 回合（共 8 个） |
| 实习 | 大四 3–5 月 | 第 43–45 回合 |
| 毕业论文答辩 | 大四 6 月 | 第 46 回合 |
| 常规 | 其余月份 | 共 24 个回合 |

## 11. 界面与交互（详见 `05-页面设计.md`）

- 六个界面：首页 / 开局页 / 游戏主界面 / 结局页 / 成就页 / 回顾页；另有**暂停覆盖层**与**回顾详情页**
- 三种推进方式：鼠标点「继续」、键盘空格/回车、自动推进（1 / 2 / 4 秒可切换）；游戏中按 ESC 或点「暂停」进入暂停页，暂停页可「继续游戏」或「结束并返回首页」（中途结束计入记录）

## 12. 边界

**不做**：动画特效、设置菜单、多语言、配置项、异常兜底、单元测试、网络功能。

**已做**：按钮点击音效（`audio/click.ogg`，用标准库 `wave` 生成后由 `pygame.mixer` 播放）。

**做**（本轮新增）：成就系统、最近 5 局的回顾与 txt 导出（仅保留近 5 局，不做长期存档）。

## 13. 待确认事项

| 序号 | 事项 | 说明 |
| --- | --- | --- |
| 1 | **天赋池 ≥30 条条目** | 写文案阶段一并设计 |
| 2 | **各结局权重** | ✅ 已标定（见 `docs/ending.md` §6 目标分布） |
| 3 | **成就条目与达成条件** | ✅ 已完成（15 条，见 `docs/achievement-list.md`） |

> 回顾记录与成就进度**跨次运行保留**（写入本地文件，人类已裁定）。